<ul class="list-group mt-4" id="webhook-credential-list">
  <% webhook_credentials.each do |credential| %>
    <%= render partial: "credential", locals: { credential: credential } %>
  <% end %>
</ul>
