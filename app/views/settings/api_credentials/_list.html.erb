<ul class="list-group mt-4" id="integration-credential-list">
  <% api_credentials.each do |credential| %>
    <%= render partial: "credential", locals: { credential: credential } %>
  <% end %>
</ul>
